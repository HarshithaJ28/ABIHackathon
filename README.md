# ABIHackathon

This project generates its own data locally when you run the pipeline.

## Run

```powershell
pip install -r requirements.txt
python pipeline.py
```

After it finishes, your generated files will be saved in `data/raw/` and `data/output/`.
